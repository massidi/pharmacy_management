{% extends 'base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block content %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('pharmacy') }}">Accueil</a></li>
        <li class="breadcrumb-item active">Client</li>
    </ol>

    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">

                    <div class="card-header card-header-primary">
                        <h4 class="card-title "> LISTE DES CLIENTS</h4>
                        <p class="card-category"> Here is a subtitle for this table</p>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table"
                                   data-toggle="table"
                                   data-search="true"
                                   data-virtual-scroll="true"
                                   data-show-columns="true">
                                <thead>
                                <tr>
                                    <th>#</th>
                                    <th data-sortable="true">Nom</th>
                                    <th>adresse</th>
{#                                    <th data-sortable="false">telephone</th>#}
                                    <th data-sortable="false">Adresse email</th>

                                </tr>
                                </thead>
                                <tbody>
                                {% for client in clients %}
                                    <tr>
                                        <td>{{ client.id }}</td>
                                        <td>{{ client.name }}</td>
                                        <td>{{ client.address}}</td>

{#                                        <td>{{ client.phoneNnumber }}</td>#}
                                        <td>{{ client.email }}</td>


{#                                        <td>#}

{#                                            {{ include('client/_delete_form.html.twig') }}#}

{#                                            <a href="{{ path('client_edit', {'id': client.id}) }}" class="btn btn-warning">#}
{#                                                <i class="material-icons">edit</i></a>#}
{#                                        </td>#}
                                    </tr>
                                {% else %}
                                    <tr>
                                        <td colspan="5" class="text-center"><b>no records found</b> </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}
